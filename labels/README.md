# Label data for URLs

This is for a simple thing to label data and export to CSV. Specifically, you import a HAR file, transform the URLs (that the script classifies as important! *not* all URLs in the file) into the format you wish to have.

For more info in the future, keep an eye on [Harambe](https://github.com/harambe-tool) - this tool is for training data.
